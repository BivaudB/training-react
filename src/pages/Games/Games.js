import React from 'react';
import Navigation from "../Navigation";

const Games = () => {
    return (
        <div>
            <Navigation />
            <h1>Voici le menu pour les jeux</h1>
        </div>
    );
};

export default Games;